Compact 32px icon-only control for toolbars and title bars.

```jsx
<IconButton icon={<Icon/>} label="Toggle sidebar" active />
```

Always pass `label` (used as `aria-label`/tooltip) since there is no visible text.
