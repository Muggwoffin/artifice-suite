Modal over a translucent ink scrim — the only place transparency is used in the system.

```jsx
<Dialog title="Discard unsaved edits?" actions={<><Button variant="secondary">Cancel</Button><Button variant="danger">Discard</Button></>}>
  This transcript has 4 unsaved corrections.
</Dialog>
```
