Plain, unapologetic empty state — no illustration, marginal-note tone.

```jsx
<EmptyState title="Nothing transcribed yet." description="Drop an audio file to begin." action={<Button>Choose file</Button>}/>
```
